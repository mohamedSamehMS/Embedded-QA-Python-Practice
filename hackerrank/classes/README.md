# Classes

Python classes are useful later for test-framework design, device abstractions, and reusable test objects.

No HackerRank problems are selected for the core set yet because the available problems are not strongly related to embedded QA.

Later practical examples can include `SerialDevice`, `TestResult`, and other reusable test objects.
