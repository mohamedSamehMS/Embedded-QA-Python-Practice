# LeetCode (Python)

Not started yet. Once active, prioritized by the same QA-relevance logic as the HackerRank folder:

```
string-parsing/     - breaking raw serial/log lines into fields
regex-matching/      - validating and extracting structured data from text
```

Lower priority for this repo's purpose: pure algorithmic categories (DP, graphs, trees) that don't map to test-scripting or log-parsing work. Those live in the C-focused repo instead, where the goal is general algorithmic fluency rather than QA-specific application.
